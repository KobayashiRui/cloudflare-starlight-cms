{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "cloudflare-starlight-cms",
  "main": "./src/index.ts",
  "compatibility_date": "2026-09-09",
  "compatibility_flags": ["nodejs_compat"],
  // Runtime Variables and Secrets are configured in the Cloudflare dashboard.
  // Keep them when Workers Builds deploys a new code version.
  "keep_vars": true,
  "workers_dev": false,
  "preview_urls": false,
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS",
    "html_handling": "auto-trailing-slash",
    "not_found_handling": "404-page",
    "run_worker_first": ["/admin", "/admin/*"]
  },
  "d1_databases": [{
    "binding": "DB",
    "migrations_dir": "./migrations"
  }],
  "r2_buckets": [{ "binding": "MEDIA" }],
  "observability": { "enabled": true, "head_sampling_rate": 1 }
}
